---
# FIRE snapshot number and simulation id (user specific)
snap : 385 
sim : 'm12b'
# Simulation directory and satellite ids location (user specific)
sim_dir : '/home/jovyan/data/fire2/m12b_res7100/'
sat_id_dir : '/home/jovyan/home/tracked/m12b/all_LMC_inds.txt'
# Min and max radius [kpc]
rmin : 20
rmax : 300
# Min and max value for healpy.newvisufunc.projview used in mollweide_plotting.py
bmin : -1
bmax : 1
# If True, creates mollweide plot of overdensity 
overdensity : True
# If True, removes the satellite using satellite ids (mutually exclusive to only_sat_mask)
sat_mask : False
# If True, keeps only the satellite using satellite ids (mutually exclusive to sat_mask)
only_sat_mask : True
# If True, removes the halo using rmin and rmax
halo_mask : True
# If True, randomly subsamples a number of particles equal to the subsample value
rand_mask : True
subsample : 10000
# Ananke parameters
ananke_name : 'sim'
photo_sys : 'padova/LSST'
cmd_magnames: 'rmag,gmag-rmag'
app_mag_lim_lo: 17
app_mag_lim_hi: 27.5
abs_mag_lim_lo: -7.0
abs_mag_lim_hi: 10.0
ananke_r_max: 1000
...